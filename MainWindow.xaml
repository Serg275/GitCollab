<Window x:Class="GitCollab.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GitCollab"
        mc:Ignorable="d"
        Title="Авторизация" Height="200" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Логин -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин:" Margin="0,0,5,5"/>
        <TextBox Grid.Row="0" Grid.Column="1" Name="LoginBox" Margin="0,0,0,5"/>

        <!-- Пароль -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Пароль:" Margin="0,0,5,5"/>
        <PasswordBox Grid.Row="1" Grid.Column="1" Name="PasswordBox" Margin="0,0,0,5"/>

        <!-- Кнопки -->
        <Button Grid.Row="2" Grid.Column="0" Content="Войти" Name="LoginButton" Margin="0,10,5,0" Click="LoginButton_Click"/>
        <Button Grid.Row="2" Grid.Column="1" Content="Регистрация" Name="RegisterButton" Margin="5,10,0,0" Click="RegisterButton_Click"/>    </Grid>
</Window>
